const capturarDatos =() =>{

    let cantidad= document.getElemementByid("lista-carrito").value;
    let boton=  document.getElemementByid("button")
    let precio = document.getElementById("calcular")
    let total = cantidad * precio

    if()
}